# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Local config & secrets -- never commit credentials
.env
backend/secrets/
.claude/

# Cached raw downloads (pipeline re-fetches on demand; ~43 MB GRACE NetCDF)
data/raw/

# ML model artifacts -- hosted on Hugging Face Hub (set HF_REPO_ID; see
# backend/models/download.py). Too large for git: the full-data downscaler RF
# is ~670 MB. Kept on disk locally for dev/scoring; never committed.
downscaler.pkl
forecaster.pt
forecaster_checkpoint.pt

# Transient run logs
*.log

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
