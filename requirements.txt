# AquaSignal ingestion pipeline -- Python 3.11
xarray>=2024.1.0
rioxarray>=0.15.0
geopandas>=0.14.0
earthengine-api>=0.1.390
requests>=2.31.0
numpy>=1.26.0
pandas>=2.1.0
pyarrow>=15.0.0
# xarray backends / accelerators required at runtime:
netCDF4>=1.6.5        # reads the GRACE-FO mascon NetCDF
bottleneck>=1.3.7     # backs xarray's ffill used for GRACE lag handling
scipy>=1.11.0         # backs xarray.interp (bilinear GRACE regridding)
shapely>=2.0.0        # grid-cell polygons for GeoJSON metadata
# monthly_cron.py extras (DB write + alert trigger; API deps live in backend/)
asyncpg>=0.30
httpx>=0.27
