# AquaSignal modelling layer -- install on top of requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
scikit-learn>=1.4.0
joblib>=1.3.0
matplotlib>=3.8.0
# Fetches the model artifacts from Hugging Face Hub (they are no longer
# committed to git -- the full-data downscaler RF is ~670 MB).
huggingface_hub>=0.23.0
